QN code base fixed and greatly extended optimization procedures:
  1. Added a generous set of AD-capable, variable-dimensioned test problems from
      1. NonlinearOptimizationTestFunctionsInJulia.jl
      1. Zygote and ForwardDiff capable.
      1. Code is planned to use "func" for cost function, "grad" for gradient (generated by Zygote), and "hess" for the hessian (generated by ForwardDiff).  
  1. Implemented QN updates in B and H mode. 
  1. Implemented std Trust Region control
  1. Implemented +def update fixer for BFGS and DFP.  Note optimization principle *only* makes sense for B and H +def.
  1. QN updates to be used with sym storage matrices.
  1. Initial Conditions
      1. Currently tested with fixed initial conditions (0.5,-0.5,...)
      1. Need to make a random start condition.  Check all test problems are defined on R^n
  1.  ToDo
    2. Still need to implement a variety of search routines.  This may need to be thought about.
    1. Still need to implement direction picking.
 
